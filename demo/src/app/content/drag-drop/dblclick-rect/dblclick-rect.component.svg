<svg:g rectangleG
       [position]="elem.position"
       [dragPosition]="elem.position"
       [color]="elem.color"
       [attr.id]="elem.$gId"
       (elemReallyClicked)="onClick()"
       (positionChanged)="onPositionChange($event)">
</svg:g>
