<svg:g rectangleG
       [position]="position"
       [color]="elem.color"
        [attr.id]="elem.$gId"
       (mousedown)="startDrag($event)"
       (mousemove)="drag($event)"
       (mouseup)="endDrag($event)"
       (click)="clickElem($event)">
</svg:g>
