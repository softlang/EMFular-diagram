<svg:g #arrow>
  <g arrow-between-boxes *ngIf="start && end"
     [id]="id"
     [start]="start"
     [end]="end"
     [text]="text"
     [arrowStyle]="arrowStyle"
     [textStyle]="textStyle"
     [textPathStyle]="textPathStyle"
  >
  </g>
></svg:g>
