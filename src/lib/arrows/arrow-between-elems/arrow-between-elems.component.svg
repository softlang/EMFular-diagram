<svg:g #arrow>
  <g arrow-boxes *ngIf="start && end"
     [id]="id"
     [start]="start"
     [end]="end"
     [arrowStyle]="arrowStyle"
     [text]="text"
     [textStyle]="textStyle"
     [textPathStyle]="textPathStyle"
  >
  </g>
></svg:g>
